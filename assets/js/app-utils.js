window.NAH_APP=(()=>{
 const $=(s,r=document)=>r.querySelector(s), $$=(s,r=document)=>[...r.querySelectorAll(s)];
 const escapeHtml=(v='')=>String(v).replaceAll('&','&amp;').replaceAll('<','&lt;').replaceAll('>','&gt;').replaceAll('"','&quot;').replaceAll("'",'&#039;');
 const todayIso=()=>{const d=new Date();return `${d.getFullYear()}-${String(d.getMonth()+1).padStart(2,'0')}-${String(d.getDate()).padStart(2,'0')}`};
 const parseDate=v=>{if(!v)return null;const d=new Date(`${v}T12:00:00`);return isNaN(d)?null:d};
 const daysUntil=(v,from=todayIso())=>{const a=parseDate(v),b=parseDate(from);return a&&b?Math.ceil((a-b)/86400000):null};
 const daysBetween=(a,b=todayIso())=>{const x=parseDate(a),y=parseDate(b);return x&&y?Math.round((y-x)/86400000):null};
 const formatDate=v=>{const d=parseDate(v);return d?new Intl.DateTimeFormat('en-US',{month:'short',day:'numeric',year:'numeric'}).format(d):'—'};
 const nextBusinessDay=(v=todayIso())=>{const d=parseDate(v)||new Date();do d.setDate(d.getDate()+1);while([0,6].includes(d.getDay()));return `${d.getFullYear()}-${String(d.getMonth()+1).padStart(2,'0')}-${String(d.getDate()).padStart(2,'0')}`};
 function toast(message,type=''){let stack=$('.toast-stack');if(!stack){stack=document.createElement('div');stack.className='toast-stack';document.body.append(stack)}const el=document.createElement('div');el.className=`toast ${type}`;el.textContent=message;stack.append(el);setTimeout(()=>el.remove(),3500)}
 function downloadText(name,text,mime='text/plain;charset=utf-8'){const blob=new Blob([text],{type:mime}),url=URL.createObjectURL(blob),a=document.createElement('a');a.href=url;a.download=name;document.body.append(a);a.click();a.remove();setTimeout(()=>URL.revokeObjectURL(url),1000)}
 const downloadJson=(name,obj)=>downloadText(name,JSON.stringify(obj,null,2),'application/json;charset=utf-8');
 async function copyText(text,msg='Copied'){try{await navigator.clipboard.writeText(text);toast(msg,'success')}catch{const ta=document.createElement('textarea');ta.value=text;document.body.append(ta);ta.select();document.execCommand('copy');ta.remove();toast(msg,'success')}}
 const csvEscape=v=>/[",\n]/.test(String(v??''))?`"${String(v??'').replaceAll('"','""')}"`:String(v??'');
 function toCsv(rows,headers){const specs=headers.map(h=>typeof h==='string'?{key:h,label:h}:h);return [specs.map(x=>csvEscape(x.label)).join(','),...rows.map(r=>specs.map(x=>csvEscape(r[x.key])).join(','))].join('\n')}
 function parseCsv(text){const rows=[];let row=[],cell='',q=false;for(let i=0;i<text.length;i++){const c=text[i],n=text[i+1];if(c==='"'&&q&&n==='"'){cell+='"';i++}else if(c==='"')q=!q;else if(c===','&&!q){row.push(cell);cell=''}else if((c==='\n'||c==='\r')&&!q){if(c==='\r'&&n==='\n')i++;row.push(cell);if(row.some(x=>x!==''))rows.push(row);row=[];cell=''}else cell+=c}row.push(cell);if(row.some(x=>x!==''))rows.push(row);if(!rows.length)return[];const headers=rows.shift().map(x=>x.trim());return rows.map(r=>Object.fromEntries(headers.map((h,i)=>[h,r[i]??''])))}
 const readFileText=f=>new Promise((res,rej)=>{const r=new FileReader();r.onload=()=>res(String(r.result));r.onerror=rej;r.readAsText(f)});
 const readFileDataUrl=f=>new Promise((res,rej)=>{const r=new FileReader();r.onload=()=>res(String(r.result));r.onerror=rej;r.readAsDataURL(f)});
 function bindTabs(root=document){$$('[data-tab]',root).forEach(btn=>btn.addEventListener('click',()=>{const group=btn.closest('[data-tabs]')||root;$$('[data-tab]',group).forEach(x=>x.classList.toggle('active',x===btn));$$('[data-panel]',group).forEach(x=>x.hidden=x.dataset.panel!==btn.dataset.tab)}))}
 function modal(title,html){let back=$('.modal-backdrop');if(!back){back=document.createElement('div');back.className='modal-backdrop';back.innerHTML='<section class="modal" role="dialog" aria-modal="true"><div class="modal-head"><h2 data-modal-title></h2><button class="modal-close" aria-label="Close">×</button></div><div data-modal-body></div></section>';document.body.append(back);back.querySelector('.modal-close').addEventListener('click',()=>back.classList.remove('open'));back.addEventListener('click',e=>{if(e.target===back)back.classList.remove('open')})}back.querySelector('[data-modal-title]').textContent=title;back.querySelector('[data-modal-body]').innerHTML=html;back.classList.add('open');return back}
 function priorityBadge(p){return `<span class="status-pill status-${String(p).toLowerCase()}">${escapeHtml(p)}</span>`}
 return {$,$$,escapeHtml,todayIso,parseDate,daysUntil,daysBetween,formatDate,nextBusinessDay,toast,downloadText,downloadJson,copyText,toCsv,parseCsv,readFileText,readFileDataUrl,bindTabs,modal,priorityBadge};
})();

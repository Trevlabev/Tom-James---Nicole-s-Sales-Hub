(()=>{const U=NAH_APP,mode=U.$('[data-storage-mode]');const state=NAH_STORE.load();mode.value=NAH_STORE.getMode();document.querySelectorAll('[data-setting]').forEach(el=>el.value=state.settings[el.dataset.setting]||'');mode.addEventListener('change',()=>{NAH_STORE.setMode(mode.value);U.toast(mode.value==='local'?'Workspace will remain on this device':'Workspace is session only','success')});U.$('[data-save-settings]').addEventListener('click',()=>{NAH_STORE.update(s=>{document.querySelectorAll('[data-setting]').forEach(el=>s.settings[el.dataset.setting]=el.value.trim());return s});U.toast('Office settings saved','success')})})();

(()=>{
 const page=document.body.dataset.page||'';const depth=Number(document.body.dataset.depth||0);const base='../'.repeat(depth);
 const nav=[['dashboard','Dashboard','index.html'],['workbench','Workbench','workbench.html'],['programs','Programs','programs/'],['trello','Trello','trello.html'],['operations','Daily Ops','operations.html'],['training','Training','training.html'],['resources','Resources','resources.html']];
 const header=`<a class="skip-link" href="#main">Skip to main content</a><header class="site-header"><div class="container nav-wrap"><a class="brand" href="${base}index.html"><span class="brand-mark">NA</span><span class="brand-copy"><strong>Nicole's Operations Suite</strong><span>Office control, training & tools</span></span></a><button class="menu-button" type="button" data-menu aria-expanded="false">Menu</button><nav class="nav-links" data-nav>${nav.map(([id,label,url])=>`<a href="${base}${url}"${page===id?' aria-current="page"':''}>${label}</a>`).join('')}</nav><div class="nav-actions"><span class="workspace-indicator"><span class="workspace-dot"></span><span data-storage-label></span></span><a class="icon-btn" href="${base}privacy.html" title="Workspace settings" aria-label="Workspace settings">⚙</a></div></div></header>`;
 const footer=`<footer><div class="container footer-grid"><div><strong>Nicole's Operations Suite</strong><p>Static GitHub Pages office toolkit. Real records stay in this browser unless explicitly exported.</p></div><div class="footer-links"><a href="${base}privacy.html">Workspace & security</a><a href="${base}embed-guide.html">Deployment</a><a href="${base}programs/">Programs</a></div></div><div class="container" style="margin-top:18px;color:rgba(255,255,255,.58);font-size:.78rem">© <span data-year></span> Internal office toolkit.</div></footer><div class="privacy-ribbon">Public deployment boundary: never hard-code client records, credentials, payment information, payroll data, or private contacts into this repository.</div>`;
 document.body.insertAdjacentHTML('afterbegin',header);document.body.insertAdjacentHTML('beforeend',footer);
 const menu=document.querySelector('[data-menu]'),navEl=document.querySelector('[data-nav]');if(menu&&navEl)menu.addEventListener('click',()=>{const open=navEl.classList.toggle('open');menu.setAttribute('aria-expanded',String(open))});
 document.querySelectorAll('[data-year]').forEach(x=>x.textContent=new Date().getFullYear());
 function updateLabel(){document.querySelectorAll('[data-storage-label]').forEach(x=>x.textContent=NAH_STORE.getMode()==='local'?'Saved on this device':'Session-only workspace')};updateLabel();window.addEventListener('nah:workspace-change',updateLabel);
 if('serviceWorker'in navigator&&location.protocol.startsWith('http'))navigator.serviceWorker.register(`${base}sw.js`).catch(()=>{});
})();

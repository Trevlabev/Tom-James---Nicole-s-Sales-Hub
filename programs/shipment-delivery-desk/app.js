(()=>{const U=NAH_APP,R=U.$('#app-root');let rows=NAH_STORE.load().records.shipments.map(x=>({...x}));const blank=()=>({id:NAH_STORE.id('ship'),client:'',recipient:'',address:'',contents:'',garmentCount:1,serviceLevel:'Ground',shipByDate:U.todayIso(),physicalStage:false,status:'Request Draft',carrier:'UPS',tracking:'',expectedDelivery:'',nextAction:'Verify address and physical staging'});R.innerHTML=`<div class="stack"><div class="grid grid-4" data-metrics></div><section class="card"><div class="card-head"><div><h2>Shipment and delivery control</h2><p>Both the digital request and the physical garment must exist.</p></div><div class="toolbar"><button class="btn btn-secondary" data-add>New shipment</button><button class="btn btn-primary" data-save>Save workspace</button><button class="btn btn-secondary" data-export>Export manifest</button></div></div><div class="table-wrap"><table><thead><tr><th>Client / recipient</th><th>Address / contents</th><th>Ship control</th><th>Carrier / tracking</th><th>Status / next</th><th>Readiness</th><th></th></tr></thead><tbody data-body></tbody></table></div></section><div class="callout warning"><strong>Two-part control:</strong> a digital request without a staged garment is incomplete; a garment on the shipping rack without a digital request is also incomplete.</div></div>`;function ready(r){const issues=[];if(!r.client)issues.push('client');if(!r.address)issues.push('address');if(!r.contents)issues.push('contents');if(!r.physicalStage)issues.push('physical garment');if(['In Transit','Delivered'].includes(r.status)&&!r.tracking)issues.push('tracking');return issues}function trackingUrl(r){if(!r.tracking)return'';const t=encodeURIComponent(r.tracking);return r.carrier==='UPS'?`https://www.ups.com/track?tracknum=${t}`:r.carrier==='FedEx'?`https://www.fedex.com/fedextrack/?trknbr=${t}`:r.carrier==='USPS'?`https://tools.usps.com/go/TrackConfirmAction?tLabels=${t}`:''}function render(){U.$('[data-metrics]').innerHTML=[['All',rows.length],['Ready to process',rows.filter(r=>!ready(r).length&&r.status==='Ready to Process').length],['In transit',rows.filter(r=>r.status==='In Transit').length],['Exceptions',rows.filter(r=>ready(r).length||r.status==='Exception').length]].map(([x,n])=>`<div class="metric-card"><div class="metric-label">${x}</div><div class="metric-value">${n}</div></div>`).join('');U.$('[data-body]').innerHTML=rows.length?rows.map(r=>`<tr><td><input data-id="${r.id}" data-k="client" value="${U.escapeHtml(r.client)}" placeholder="Client"><input data-id="${r.id}" data-k="recipient" value="${U.escapeHtml(r.recipient)}" placeholder="Recipient"></td><td><textarea data-id="${r.id}" data-k="address" style="min-height:60px">${U.escapeHtml(r.address)}</textarea><input data-id="${r.id}" data-k="contents" value="${U.escapeHtml(r.contents)}" placeholder="Contents"><input type="number" min="1" data-id="${r.id}" data-k="garmentCount" value="${r.garmentCount}"></td><td><input type="date" data-id="${r.id}" data-k="shipByDate" value="${r.shipByDate}"><select data-id="${r.id}" data-k="serviceLevel"><option>Ground</option><option${r.serviceLevel==='2-Day'?' selected':''}>2-Day</option><option${r.serviceLevel==='Overnight'?' selected':''}>Overnight</option><option${r.serviceLevel==='Local delivery'?' selected':''}>Local delivery</option><option${r.serviceLevel==='Client pickup'?' selected':''}>Client pickup</option></select><label class="checkbox-row"><input type="checkbox" data-id="${r.id}" data-k="physicalStage" ${r.physicalStage?'checked':''}><span>Physical item staged</span></label></td><td><select data-id="${r.id}" data-k="carrier"><option>UPS</option><option${r.carrier==='FedEx'?' selected':''}>FedEx</option><option${r.carrier==='USPS'?' selected':''}>USPS</option><option${r.carrier==='Local / Pickup'?' selected':''}>Local / Pickup</option></select><input data-id="${r.id}" data-k="tracking" value="${U.escapeHtml(r.tracking)}" placeholder="Tracking">${trackingUrl(r)?`<a class="text-link" target="_blank" rel="noopener" href="${trackingUrl(r)}">Track →</a>`:''}</td><td><select data-id="${r.id}" data-k="status">${['Request Draft','Ready to Process','Processed','In Transit','Out for Delivery','Delivered','Exception','Closed'].map(x=>`<option${r.status===x?' selected':''}>${x}</option>`).join('')}</select><input data-id="${r.id}" data-k="nextAction" value="${U.escapeHtml(r.nextAction)}"><input type="date" data-id="${r.id}" data-k="expectedDelivery" value="${r.expectedDelivery||''}"></td><td><span class="badge ${ready(r).length?'badge-red':'badge-green'}">${ready(r).length?`Missing ${ready(r).join(', ')}`:'Ready'}</span></td><td><button class="btn btn-danger btn-small" data-remove="${r.id}">Remove</button></td></tr>`).join(''):'<tr><td colspan="7"><div class="empty-state">No shipments yet.</div></td></tr>'}
R.addEventListener('change',e=>{if(e.target.dataset.id){const r=rows.find(x=>x.id===e.target.dataset.id);r[e.target.dataset.k]=e.target.type==='checkbox'?e.target.checked:e.target.type==='number'?Number(e.target.value):e.target.value;render()}});R.addEventListener('click',e=>{if(e.target.closest('[data-add]')){rows.unshift(blank());render()}const rm=e.target.closest('[data-remove]');if(rm){rows=rows.filter(x=>x.id!==rm.dataset.remove);render()}if(e.target.closest('[data-save]')){rows.forEach(r=>NAH_STORE.upsert('shipments',r,`Updated shipment: ${r.client||'record'}`));U.toast('Shipments saved','success')}if(e.target.closest('[data-export]'))U.downloadText(`shipment-manifest-${U.todayIso()}.csv`,U.toCsv(rows,['client','recipient','address','contents','garmentCount','serviceLevel','shipByDate','physicalStage','status','carrier','tracking','expectedDelivery','nextAction']),'text/csv;charset=utf-8')});render()})();
